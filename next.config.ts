import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: false,
  // Membiarkan Turbopack berjalan dengan konfigurasi default
  turbopack: {},
};

export default nextConfig;